name: 'build-test'
on: # rebuild any PRs and main branch changes
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  build: # make sure build/ci work properly
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
            
      - name: Setup PNPM
        uses: pnpm/action-setup@v2.1.0
        with:
          version: latest

      - run: |
          pnpm install
      - run: |
          pnpm run all
  test_specified_months:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        id: version
        with:
          token: ${{ github.token }}
          tag_prefix: ${{ github.ref_name }}/v
          release_months: |
            July
            January
      - run: git ls-remote --tags | grep refs/tags/${{ steps.version.outputs.new_tag }}
      - uses: actions/github-script@v6
        with:
          script: |
            github.rest.git.deleteRef({
              ...context.repo,
              ref: 'tags/${{ steps.version.outputs.new_tag }}',
            })
  test_unspecified_months:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        id: version
        with:
          token: ${{ github.token }}
          tag_prefix: ${{ github.ref_name }}/v
      - run: git ls-remote --tags | grep "refs/tags/${{ github.ref_name }}/v""$(date '+%Y.%m')"".0"
      - uses: actions/github-script@v6
        with:
          script: |
            github.rest.git.deleteRef({
              ...context.repo,
              ref: 'tags/${{ steps.version.outputs.new_tag }}',
            })
